def mul (x, y)
	return x * y
	
def output (name, x)
	left_to_live="""
Hey {}!
You have {} years left to live
	return left_to_live
	
def main ():
	name=
	z=
	y=
	
	x=mul (int(z), int(y))
	left_to_live=output(name, x)
	print ""
main ()
